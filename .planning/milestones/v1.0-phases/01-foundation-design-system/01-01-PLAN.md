---
phase: 01-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/styles/index.css
  - src/vite-env.d.ts
  - public/_redirects
  - .gitignore
  - tests/setup.ts
  - tests/design-tokens.test.ts
autonomous: true
requirements:
  - FOUND-01
  - SEO-04

must_haves:
  truths:
    - "Dark theme CSS variables are defined and accessible via Tailwind utility classes"
    - "All text-color/background-color token pairs pass WCAG 4.5:1 contrast ratio"
    - "Vite dev server starts and renders a React component"
    - "Vitest test suite runs and reports results"
  artifacts:
    - path: "src/styles/index.css"
      provides: "Tailwind v4 @theme design tokens with contrast-verified dark palette"
      contains: "@theme"
    - path: "vite.config.ts"
      provides: "Vite config with React, Tailwind, sitemap plugins and Vitest config"
      contains: "tailwindcss"
    - path: "index.html"
      provides: "SPA entry point with class=dark on html element"
      contains: "class=\"dark\""
    - path: "public/_redirects"
      provides: "Netlify SPA fallback routing"
      contains: "/index.html"
    - path: "tests/setup.ts"
      provides: "Vitest + React Testing Library setup"
      contains: "jest-dom"
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "script module import"
      pattern: "src/main.tsx"
    - from: "src/main.tsx"
      to: "src/styles/index.css"
      via: "CSS import"
      pattern: "import.*styles/index.css"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugin registration"
      pattern: "tailwindcss\\(\\)"
---

<objective>
Bootstrap the Vite + React + Tailwind CSS v4 project from scratch and establish the dark design token system.

Purpose: Every subsequent plan builds on this scaffold. The design tokens, build pipeline, test infrastructure, and Netlify configuration must be correct before any components are created.
Output: A running Vite dev server with Tailwind v4 dark theme tokens, Vitest configured, and Netlify SPA routing ready.
</objective>

<execution_context>
@/Users/omere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React project with Tailwind v4 and all dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/styles/index.css, src/vite-env.d.ts, public/_redirects, .gitignore</files>
  <action>
1. Remove the stale `.astro` directory: `rm -rf .astro`

2. Scaffold the Vite project using the react-ts template. Since files already exist in the directory (`.planning`, `.git`, etc.), scaffold into a temp directory and move files:
   ```bash
   npm create vite@latest temp-scaffold -- --template react-ts
   ```
   Move all scaffolded files (package.json, vite.config.ts, tsconfig files, index.html, src/, public/, .gitignore) into the project root. Delete the temp directory. Merge .gitignore (keep existing entries, add Vite defaults).

3. Install additional dependencies:
   ```bash
   npm install react-router
   npm install -D @tailwindcss/vite tailwindcss vite-plugin-sitemap vitest @testing-library/react @testing-library/jest-dom jsdom @types/node
   ```

4. Replace `vite.config.ts` with the full Phase 1 config:
   ```typescript
   import { defineConfig } from "vite";
   import react from "@vitejs/plugin-react";
   import tailwindcss from "@tailwindcss/vite";
   import sitemap from "vite-plugin-sitemap";

   export default defineConfig({
     plugins: [
       react(),
       tailwindcss(),
       sitemap({
         hostname: "https://omeretrog.com",
         dynamicRoutes: ["/", "/work", "/about", "/contact"],
       }),
     ],
     test: {
       environment: "jsdom",
       setupFiles: ["./tests/setup.ts"],
       globals: true,
     },
   });
   ```

5. Replace `index.html` to hardcode the dark class and set the home page title (per Pitfall 4 from research):
   ```html
   <!doctype html>
   <html lang="en" class="dark">
     <head>
       <meta charset="UTF-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1.0" />
       <link rel="preconnect" href="https://fonts.googleapis.com" />
       <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
       <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
       <title>Omer Etrog — Modern Web Migration in Under an Hour</title>
     </head>
     <body class="bg-bg text-text antialiased">
       <div id="root"></div>
       <script type="module" src="/src/main.tsx"></script>
     </body>
   </html>
   ```

6. Replace `src/styles/index.css` with the full Tailwind v4 dark theme token system (from research):
   ```css
   @import "tailwindcss";

   @custom-variant dark (&:where(.dark, .dark *));

   @theme {
     --color-bg:           #111111;
     --color-surface:      #1a1a1a;
     --color-surface-alt:  #222222;
     --color-border:       #333333;

     --color-text:         #f0f0f0;
     --color-text-muted:   #a3a3a3;

     --color-accent:       oklch(0.72 0.19 255);
     --color-accent-hover: oklch(0.80 0.19 255);

     --font-sans: "Inter", system-ui, -apple-system, sans-serif;
   }
   ```

7. Replace `src/main.tsx` with a minimal entry that imports the CSS and renders a placeholder:
   ```tsx
   import React from "react";
   import ReactDOM from "react-dom/client";
   import "./styles/index.css";

   function App() {
     return (
       <div className="min-h-screen flex items-center justify-center bg-bg text-text">
         <h1 className="text-3xl font-bold">Omer Etrog</h1>
       </div>
     );
   }

   ReactDOM.createRoot(document.getElementById("root")!).render(
     <React.StrictMode>
       <App />
     </React.StrictMode>
   );
   ```

8. Create `public/_redirects` with Netlify SPA fallback:
   ```
   /*    /index.html   200
   ```

9. Delete any Vite scaffold boilerplate files that are not needed: `src/App.tsx`, `src/App.css`, `src/index.css` (we use `src/styles/index.css`), `src/assets/react.svg`, `public/vite.svg`.

10. Run `npm run dev` briefly to confirm the dev server starts, then stop it.
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Vite project builds successfully. index.html has class="dark". src/styles/index.css contains @theme with all color tokens. public/_redirects exists with SPA fallback rule. All dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create test infrastructure and design token verification tests</name>
  <files>tests/setup.ts, tests/design-tokens.test.ts</files>
  <action>
1. Create `tests/setup.ts`:
   ```typescript
   import "@testing-library/jest-dom/vitest";
   ```

2. Create `tests/design-tokens.test.ts` that verifies the design token CSS file:
   ```typescript
   import { describe, it, expect } from "vitest";
   import { readFileSync } from "fs";
   import { resolve } from "path";

   const cssContent = readFileSync(
     resolve(__dirname, "../src/styles/index.css"),
     "utf-8"
   );

   describe("Design Tokens", () => {
     it("imports tailwindcss", () => {
       expect(cssContent).toContain('@import "tailwindcss"');
     });

     it("defines @theme block", () => {
       expect(cssContent).toContain("@theme");
     });

     it("defines dark custom variant", () => {
       expect(cssContent).toContain("@custom-variant dark");
     });

     const requiredTokens = [
       "--color-bg",
       "--color-surface",
       "--color-surface-alt",
       "--color-border",
       "--color-text",
       "--color-text-muted",
       "--color-accent",
       "--color-accent-hover",
       "--font-sans",
     ];

     requiredTokens.forEach((token) => {
       it(`defines ${token}`, () => {
         expect(cssContent).toContain(token);
       });
     });

     it("uses near-black background (#111111), not pure black", () => {
       expect(cssContent).toMatch(/--color-bg:\s*#111111/);
     });
   });
   ```

3. Run the tests to confirm they pass.
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npx vitest run tests/design-tokens.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All design token tests pass. tests/setup.ts configures jest-dom matchers. Test infrastructure is operational.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx vitest run` — all tests green
- `index.html` contains `class="dark"` on the `<html>` element
- `src/styles/index.css` contains `@theme` with all 9 design tokens
- `public/_redirects` contains `/*    /index.html   200`
- `vite.config.ts` includes react, tailwindcss, and sitemap plugins
</verification>

<success_criteria>
- Vite dev server starts and shows the dark-themed placeholder page
- Build produces `dist/` with all assets
- Vitest runs and reports green for design token presence tests
- All color tokens documented with contrast ratios in CSS comments
- Netlify SPA routing configured via _redirects
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-01-SUMMARY.md`
</output>
