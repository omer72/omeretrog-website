---
phase: 02-core-pages-content
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/home/HeroSection.tsx
  - src/components/home/HowItWorksSection.tsx
  - src/components/home/TestimonialsSection.tsx
  - src/pages/HomePage.tsx
  - tests/hero-section.test.tsx
  - tests/how-it-works-section.test.tsx
  - tests/testimonials-section.test.tsx
  - tests/home-page.test.tsx
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, TEST-01, TEST-02, TEST-03, SEO-03]

must_haves:
  truths:
    - "Hero section displays bold value proposition and primary CTA linking to /contact"
    - "Social proof teaser (testimonial snippet) is visible in or immediately below the hero"
    - "How It Works section renders exactly 3 steps"
    - "Testimonials section renders at least 2-3 client quotes with names and specific results"
    - "Testimonials are visible on the home page, not buried in navigation"
    - "HomePage includes JSON-LD Service schema in a script tag"
  artifacts:
    - path: "src/components/home/HeroSection.tsx"
      provides: "Hero with value prop, CTA, and testimonial teaser"
      min_lines: 25
    - path: "src/components/home/HowItWorksSection.tsx"
      provides: "3-step process section"
      min_lines: 20
    - path: "src/components/home/TestimonialsSection.tsx"
      provides: "Client testimonial cards"
      min_lines: 20
    - path: "src/pages/HomePage.tsx"
      provides: "Thin page composer importing all sections + JsonLd"
      min_lines: 20
    - path: "tests/hero-section.test.tsx"
      provides: "Tests for HERO-01, HERO-02, HERO-03"
    - path: "tests/how-it-works-section.test.tsx"
      provides: "Tests for HERO-04"
    - path: "tests/testimonials-section.test.tsx"
      provides: "Tests for TEST-01, TEST-02"
    - path: "tests/home-page.test.tsx"
      provides: "Tests for TEST-03, SEO-03"
  key_links:
    - from: "src/pages/HomePage.tsx"
      to: "src/components/home/HeroSection.tsx"
      via: "import and render"
      pattern: "import HeroSection"
    - from: "src/pages/HomePage.tsx"
      to: "src/components/home/TestimonialsSection.tsx"
      via: "import and render with testimonials prop"
      pattern: "TestimonialsSection.*testimonials"
    - from: "src/pages/HomePage.tsx"
      to: "src/components/JsonLd.tsx"
      via: "import JsonLd + serviceSchema"
      pattern: "JsonLd.*data.*serviceSchema"
    - from: "src/components/home/HeroSection.tsx"
      to: "/contact"
      via: "React Router Link CTA"
      pattern: "Link.*to.*contact"
---

<objective>
Build all HomePage section components (Hero, HowItWorks, Testimonials), wire them into HomePage as a thin composer with JSON-LD, and write tests covering HERO-01 through TEST-03 and SEO-03.

Purpose: The homepage is the first thing visitors see. It must immediately communicate the service, show social proof, explain the process, and include structured data for Google.
Output: 3 section components, updated HomePage, 4 test files.
</objective>

<execution_context>
@/Users/omere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages-content/02-RESEARCH.md
@.planning/phases/02-core-pages-content/02-01-SUMMARY.md

@src/styles/index.css
@src/pages/HomePage.tsx
@src/router.tsx

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/content/testimonials.ts:
```typescript
export interface Testimonial {
  id: string;
  quote: string;
  clientName: string;
  result: string;
  role?: string;
}
export const testimonials: Testimonial[];
```

From src/content/how-it-works.ts:
```typescript
export interface HowItWorksStep {
  step: number;
  title: string;
  description: string;
}
export const steps: HowItWorksStep[];
```

From src/content/schema.ts:
```typescript
export const serviceSchema: Record<string, unknown>;
```

From src/components/JsonLd.tsx:
```typescript
export default function JsonLd({ data }: { data: Record<string, unknown> }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build HeroSection, HowItWorksSection, TestimonialsSection components with tests</name>
  <files>src/components/home/HeroSection.tsx, src/components/home/HowItWorksSection.tsx, src/components/home/TestimonialsSection.tsx, tests/hero-section.test.tsx, tests/how-it-works-section.test.tsx, tests/testimonials-section.test.tsx</files>
  <behavior>
    - HeroSection renders an h1 containing "modern" and "under an hour" (HERO-01)
    - HeroSection renders a Link to /contact with CTA text like "Get Started" (HERO-02)
    - HeroSection renders the testimonial teaser (clientName and quote from first testimonial) (HERO-03)
    - HowItWorksSection renders all 3 steps with step numbers, titles, and descriptions (HERO-04)
    - TestimonialsSection renders at least 2 testimonial quotes (TEST-01)
    - TestimonialsSection shows clientName and result for each testimonial (TEST-02)
  </behavior>
  <action>
Write tests FIRST (RED), then implement components (GREEN).

**Tests (write first):**

`tests/hero-section.test.tsx`: Wrap in MemoryRouter. Test: (1) h1 contains value proposition text, (2) link with href="/contact" exists, (3) testimonial teaser clientName is rendered. Pass a single mock `Testimonial` as `testimonialTeaser` prop.

`tests/how-it-works-section.test.tsx`: Test: (1) renders heading "How It Works", (2) renders all 3 step titles from mock data, (3) renders step numbers.

`tests/testimonials-section.test.tsx`: Test: (1) renders all quotes from mock array, (2) renders clientName for each, (3) renders result for each (TEST-02).

**Components (implement after tests):**

`src/components/home/HeroSection.tsx`:
- Props: `{ testimonialTeaser: Testimonial }` — import Testimonial type from `../content/testimonials`.
- Full-width section with `mx-auto max-w-4xl px-4 py-24 md:px-8 md:py-32`.
- h1 with `text-5xl font-bold leading-tight md:text-6xl`. Include accent-colored span for "in under an hour".
- Subtitle paragraph with `text-lg text-text-muted`.
- Primary CTA: `<Link to="/contact">` styled as `bg-accent text-bg px-6 py-3 rounded-lg font-semibold hover:bg-accent-hover transition-colors`.
- Testimonial teaser below CTA: small card or quote showing `testimonialTeaser.clientName` and truncated `testimonialTeaser.quote` with `text-text-muted text-sm`.
- One primary CTA only — no competing CTAs (anti-pattern from research).

`src/components/home/HowItWorksSection.tsx`:
- Props: `{ steps: HowItWorksStep[] }`.
- Section with `bg-surface-alt py-20`.
- h2 "How It Works" centered.
- Responsive 3-column grid (`grid md:grid-cols-3 gap-8`).
- Each step: card with `rounded-xl border border-border bg-surface p-6`, step number in `text-4xl font-bold text-accent`, title in h3, description in `text-text-muted`.

`src/components/home/TestimonialsSection.tsx`:
- Props: `{ testimonials: Testimonial[] }`.
- Section with `py-20`.
- h2 "What Clients Say" centered.
- Grid of testimonial cards (`grid md:grid-cols-2 lg:grid-cols-3 gap-6`).
- Each card: `rounded-xl border border-border bg-surface p-6`. Quote in blockquote, clientName in bold, result in `text-text-muted text-sm`, optional role below name.

Use `Link` from `react-router` (not `react-router-dom`). Import from `react-router`.
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npx vitest run tests/hero-section.test.tsx tests/how-it-works-section.test.tsx tests/testimonials-section.test.tsx</automated>
  </verify>
  <done>All 3 section components render correctly. Tests pass for HERO-01 (value prop h1), HERO-02 (CTA to /contact), HERO-03 (testimonial teaser), HERO-04 (3 steps), TEST-01 (2+ testimonials), TEST-02 (name + result per testimonial).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire HomePage as thin composer with JSON-LD and write integration tests</name>
  <files>src/pages/HomePage.tsx, tests/home-page.test.tsx</files>
  <behavior>
    - HomePage renders TestimonialsSection (TEST-03 — testimonials visible on home page)
    - HomePage renders a script[type="application/ld+json"] element (SEO-03)
    - HomePage renders HeroSection and HowItWorksSection
    - HomePage stays thin (under 40 lines)
  </behavior>
  <action>
Write test FIRST (RED), then update HomePage (GREEN).

**Test (write first):**

`tests/home-page.test.tsx`: Wrap in MemoryRouter. Test: (1) renders TestimonialsSection content — check for a known testimonial clientName from the real data file, (2) renders a `script[type="application/ld+json"]` element (SEO-03), (3) renders "How It Works" heading, (4) renders hero h1 value proposition.

**HomePage update:**

Rewrite `src/pages/HomePage.tsx` as a thin composer (Pattern 2 from research):
- Keep existing `<title>`, `<meta name="description">`, `<link rel="canonical">` tags.
- Import and render: `HeroSection` (pass `testimonials[0]` as teaser), `HowItWorksSection` (pass `steps`), `TestimonialsSection` (pass `testimonials`).
- Import and render: `JsonLd` with `serviceSchema` data prop.
- Import data from `../content/testimonials`, `../content/how-it-works`, `../content/schema`.
- Wrap sections in a Fragment `<>...</>`, NOT a wrapping div or section (the page is a composer, not a container).
- File should be under 40 lines total.

Do NOT hardcode any content in HomePage. All content comes from data imports.
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npx vitest run tests/home-page.test.tsx && npx vitest run</automated>
  </verify>
  <done>HomePage imports and renders all 3 sections plus JsonLd. TEST-03 satisfied (testimonials on home page). SEO-03 satisfied (JSON-LD script tag present). Full test suite passes (all Phase 1 + Phase 2 tests).</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass (Phase 1 existing 29 + new Phase 2 tests)
- `npx vitest run tests/hero-section.test.tsx tests/how-it-works-section.test.tsx tests/testimonials-section.test.tsx tests/home-page.test.tsx` — all 4 new test files green
- HomePage.tsx is under 40 lines
</verification>

<success_criteria>
- Hero section shows value proposition, CTA to /contact, and testimonial teaser above the fold
- How It Works section renders 3 steps in a responsive grid
- Testimonials section renders 2-3 client quotes with names and specific results
- All content driven by TypeScript data files (no hardcoded strings in components)
- JSON-LD Service schema rendered on homepage
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-content/02-02-SUMMARY.md`
</output>
