---
phase: 02-core-pages-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/testimonials.ts
  - src/content/how-it-works.ts
  - src/content/schema.ts
  - src/components/JsonLd.tsx
  - index.html
autonomous: true
requirements: [SEO-02, SEO-03]

must_haves:
  truths:
    - "TypeScript content interfaces exist with required fields before any component is built"
    - "OG meta tags are present in static index.html for social crawlers"
    - "JSON-LD component exists and safely serializes structured data"
  artifacts:
    - path: "src/content/testimonials.ts"
      provides: "Testimonial interface and data array"
      exports: ["Testimonial", "testimonials"]
    - path: "src/content/how-it-works.ts"
      provides: "HowItWorksStep interface and steps array"
      exports: ["HowItWorksStep", "steps"]
    - path: "src/content/schema.ts"
      provides: "Service JSON-LD schema object"
      exports: ["serviceSchema"]
    - path: "src/components/JsonLd.tsx"
      provides: "Reusable JSON-LD injection component"
      exports: ["default"]
    - path: "index.html"
      provides: "Static OG meta tags for social sharing"
      contains: "og:image"
  key_links:
    - from: "src/content/testimonials.ts"
      to: "components/home/HeroSection.tsx (Plan 02)"
      via: "Testimonial type import"
      pattern: "import.*Testimonial.*from.*content/testimonials"
    - from: "src/content/schema.ts"
      to: "src/components/JsonLd.tsx"
      via: "data prop accepts Record<string, unknown>"
      pattern: "JsonLd.*data.*serviceSchema"
---

<objective>
Define all TypeScript content interfaces and data files, create the JsonLd utility component, and add static Open Graph tags to index.html.

Purpose: Content data shapes MUST exist before section components are built (per research finding). OG tags must be in static HTML because social crawlers don't execute JS.
Output: 3 content files, 1 utility component, updated index.html with OG tags.
</objective>

<execution_context>
@/Users/omere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages-content/02-RESEARCH.md

@src/styles/index.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript content data files and interfaces</name>
  <files>src/content/testimonials.ts, src/content/how-it-works.ts, src/content/schema.ts</files>
  <action>
Create `src/content/` directory with three files:

1. `src/content/testimonials.ts`:
   - Export `Testimonial` interface with fields: `id: string`, `quote: string`, `clientName: string`, `result: string` (required — satisfies TEST-02), `role?: string` (optional).
   - Export `testimonials: Testimonial[]` with 3 entries. Use realistic placeholder content with a `// TODO: replace with real client quotes once permission obtained` comment. Each entry MUST have a non-empty `result` field with a specific measurable outcome (e.g., "Full Wix migration + launch in under 45 minutes").

2. `src/content/how-it-works.ts`:
   - Export `HowItWorksStep` interface with fields: `step: number`, `title: string`, `description: string`.
   - Export `steps: HowItWorksStep[]` with exactly 3 entries following the research pattern: (1) Share your current site, (2) We migrate everything, (3) Launch your new site.

3. `src/content/schema.ts`:
   - Export `serviceSchema` as a plain object (type `Record<string, unknown>`) containing JSON-LD Service schema per research Pattern 4. Include `@context`, `@type: "Service"`, `provider` with `@type: "Person"` and `name: "Omer Etrog"`, `serviceType: "Web Development"`, `url: "https://omeretrog.com"`.

All files use TypeScript with explicit type annotations. No default exports — use named exports only.
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npx tsc --noEmit src/content/testimonials.ts src/content/how-it-works.ts src/content/schema.ts 2>&1 | head -20</automated>
  </verify>
  <done>Three content files exist with exported interfaces and typed data arrays. TypeScript compiles with no errors. Testimonial interface has required `result` field.</done>
</task>

<task type="auto">
  <name>Task 2: Create JsonLd component and add static OG tags to index.html</name>
  <files>src/components/JsonLd.tsx, index.html</files>
  <action>
1. Create `src/components/JsonLd.tsx`:
   - Accept props `{ data: Record<string, unknown> }`.
   - Serialize with `JSON.stringify(data).replace(/&lt;/g, "\\u003c")` for XSS safety (Pitfall 3 from research).
   - Render `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: json }} />`.
   - Export as default.

2. Update `index.html` `<head>` section — add AFTER the existing `<title>` tag:
   ```html
   <meta property="og:type" content="website" />
   <meta property="og:url" content="https://omeretrog.com/" />
   <meta property="og:title" content="Omer Etrog | Modern Web Migration in Under an Hour" />
   <meta property="og:description" content="Transform your outdated Wix site into a fast, modern website. Full content migration in under an hour." />
   <meta property="og:image" content="https://omeretrog.com/og-image.jpg" />
   <meta name="twitter:card" content="summary_large_image" />
   <meta name="twitter:image" content="https://omeretrog.com/og-image.jpg" />
   ```

   Also create a placeholder `public/og-image.jpg` — generate a simple dark-themed 1200x630 SVG placeholder saved as `public/og-image-placeholder.svg` with text "OG Image — Replace before launch" and a `// TODO` comment in index.html noting the image needs to be replaced with a real designed OG image.

Do NOT use react-helmet. Do NOT use useEffect for metadata injection. The OG tags go in static HTML because social crawlers don't execute JS (Pitfall 2).
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && grep -c "og:image" index.html && npx tsc --noEmit src/components/JsonLd.tsx 2>&1 | head -10</automated>
  </verify>
  <done>index.html contains all 7 OG/Twitter meta tags. JsonLd.tsx compiles without errors and exports a default component. OG image placeholder exists.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new files
- `grep "og:image" index.html` returns matches
- `grep "og:title" index.html` returns matches
- Content files export correct named exports
</verification>

<success_criteria>
- All 3 content data files exist with typed interfaces and data
- JsonLd component exists with XSS-safe serialization
- index.html has static OG tags for social sharing (SEO-02)
- No new npm dependencies installed
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-content/02-01-SUMMARY.md`
</output>
