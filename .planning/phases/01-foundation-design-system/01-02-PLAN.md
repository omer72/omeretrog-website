---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/main.tsx
  - src/router.tsx
  - src/components/layout/RootLayout.tsx
  - src/components/layout/Nav.tsx
  - src/components/layout/Footer.tsx
  - src/pages/HomePage.tsx
  - src/pages/WorkPage.tsx
  - src/pages/AboutPage.tsx
  - src/pages/ContactPage.tsx
  - src/pages/NotFoundPage.tsx
  - tests/nav.test.tsx
  - tests/seo.test.tsx
  - tests/layout.test.tsx
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - SEO-01
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "Navigation renders links to Home, Work, About, and Contact on all viewport sizes"
    - "Footer with contact info and social links appears on every page"
    - "Each page has a unique keyword-targeted title and meta description"
    - "Every page has exactly one h1 and uses semantic HTML landmarks"
    - "sitemap.xml and robots.txt are generated at build time"
    - "Layout is responsive at 375px, 768px, and 1280px widths"
  artifacts:
    - path: "src/router.tsx"
      provides: "Single source of routing truth with all 4 pages + 404"
      exports: ["router"]
    - path: "src/components/layout/RootLayout.tsx"
      provides: "Persistent Nav + main + Footer shell"
      contains: "Outlet"
    - path: "src/components/layout/Nav.tsx"
      provides: "Responsive navigation with NavLink active states and mobile toggle"
      contains: "NavLink"
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer with contact info and social links"
      contains: "footer"
    - path: "src/pages/HomePage.tsx"
      provides: "Home page with unique SEO metadata and h1"
      contains: "<title>"
    - path: "src/pages/WorkPage.tsx"
      provides: "Work page stub with unique SEO metadata and h1"
      contains: "<title>"
    - path: "src/pages/AboutPage.tsx"
      provides: "About page stub with unique SEO metadata and h1"
      contains: "<title>"
    - path: "src/pages/ContactPage.tsx"
      provides: "Contact page stub with unique SEO metadata and h1"
      contains: "<title>"
    - path: "tests/nav.test.tsx"
      provides: "Nav link presence tests"
      contains: "NavLink"
    - path: "tests/seo.test.tsx"
      provides: "Per-page title and h1 tests"
      contains: "title"
  key_links:
    - from: "src/main.tsx"
      to: "src/router.tsx"
      via: "RouterProvider import"
      pattern: "RouterProvider"
    - from: "src/router.tsx"
      to: "src/components/layout/RootLayout.tsx"
      via: "layout route element"
      pattern: "RootLayout"
    - from: "src/components/layout/RootLayout.tsx"
      to: "src/components/layout/Nav.tsx"
      via: "component import"
      pattern: "import Nav"
    - from: "src/components/layout/RootLayout.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "component import"
      pattern: "import Footer"
    - from: "src/components/layout/Nav.tsx"
      to: "react-router"
      via: "NavLink with end prop on home route"
      pattern: 'NavLink.*end'
---

<objective>
Build the layout system, responsive navigation, footer, all page stubs with SEO metadata, and the React Router configuration.

Purpose: After this plan, every page renders inside a shared layout with persistent Nav and Footer, has unique SEO metadata, uses semantic HTML, and the sitemap is generated at build time. This is the foundation all content phases build on.
Output: Complete site shell with 4 routable pages, responsive nav with mobile toggle, footer, per-page SEO, and full test coverage.
</objective>

<execution_context>
@/Users/omere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-VALIDATION.md

<!-- Plan 01 creates the scaffold this plan builds on -->
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — key files this plan depends on -->

From src/styles/index.css (design tokens available as Tailwind utilities):
- bg-bg, bg-surface, bg-surface-alt — background colors
- text-text, text-text-muted — text colors
- text-accent, text-accent-hover — brand accent
- border-border — border color
- font-sans — Inter font stack
- Arbitrary value syntax also works: bg-[--color-bg], text-[--color-text], etc.

From vite.config.ts:
- sitemap plugin configured with hostname "https://omeretrog.com" and routes ["/", "/work", "/about", "/contact"]
- Vitest configured with jsdom environment and tests/setup.ts

From index.html:
- <html lang="en" class="dark"> — dark mode always active
- <div id="root"></div> — React mount point
- Inter font loaded from Google Fonts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create router, layout components, and all page stubs with SEO metadata</name>
  <files>src/router.tsx, src/components/layout/RootLayout.tsx, src/components/layout/Nav.tsx, src/components/layout/Footer.tsx, src/pages/HomePage.tsx, src/pages/WorkPage.tsx, src/pages/AboutPage.tsx, src/pages/ContactPage.tsx, src/pages/NotFoundPage.tsx, src/main.tsx</files>
  <action>
1. Create `src/components/layout/Nav.tsx` — Responsive navigation:
   - Desktop: horizontal nav links (Home, Work, About, Contact) with NavLink active styling
   - Mobile: hamburger button toggling a vertical menu. Use React useState for open/close state
   - Use `end` prop on the Home (`/`) NavLink to prevent false active state (Pitfall 6)
   - Sticky header with backdrop blur: `sticky top-0 z-50 border-b border-border bg-bg/90 backdrop-blur-sm`
   - Logo/brand link on the left: "Omer Etrog" linking to `/`
   - `aria-label="Main navigation"` on the `<nav>` element
   - Mobile breakpoint: show hamburger below `md:` (768px), show horizontal links at `md:` and above
   - Hamburger button: `aria-expanded` attribute, `aria-controls` pointing to mobile menu id
   - Mobile menu: full-width dropdown below header with nav links, closes on link click

2. Create `src/components/layout/Footer.tsx`:
   - Semantic `<footer>` element with `role` implied
   - Contact info: email link (mailto:omer@omeretrog.com — placeholder, will be updated)
   - Social links: LinkedIn, GitHub (use placeholder URLs for now, wrapped in `<a>` with `target="_blank" rel="noopener noreferrer"`)
   - Copyright notice: "2026 Omer Etrog. All rights reserved."
   - Responsive: single column on mobile, multi-column on desktop
   - Use `bg-surface` for visual separation from main content, `border-t border-border` top border

3. Create `src/components/layout/RootLayout.tsx`:
   - Follows the exact pattern from research: `<div className="min-h-screen flex flex-col bg-bg text-text">` wrapping Nav, `<main id="main-content" className="flex-1">` with `<Outlet />`, and Footer
   - Semantic: `<main>` landmark wraps the Outlet
   - Skip-to-content link: Hidden `<a href="#main-content">` that appears on focus for accessibility

4. Create all 4 page components using React 19 native metadata hoisting. Each page MUST have:
   - Unique `<title>` with keyword targeting
   - Unique `<meta name="description">` (keyword-targeted, 150-160 chars)
   - `<link rel="canonical">` with full URL
   - Exactly ONE `<h1>` element
   - Semantic wrapper: `<section aria-labelledby="[page]-heading">` with `id` on h1
   - Placeholder content paragraph explaining what will go on the page

   Specific SEO content per page:
   - **HomePage**: title="Omer Etrog | Modern Web Migration in Under an Hour", desc="Transform your outdated Wix site into a fast, modern website. Full content migration in under an hour. See real before & after results.", h1="Your old site, rebuilt modern — in under an hour"
   - **WorkPage**: title="Portfolio | Omer Etrog Web Migration", desc="See real before and after website transformations. Outdated Wix sites turned into fast, modern websites with all content preserved.", h1="Before & After"
   - **AboutPage**: title="About Omer Etrog | Web Migration Specialist", desc="Meet Omer Etrog — web migration specialist who transforms outdated Wix sites into modern, high-performance websites.", h1="About Omer"
   - **ContactPage**: title="Contact | Get Your Site Modernized Today", desc="Ready to transform your outdated website? Contact Omer Etrog for a free consultation. Full migration in under an hour.", h1="Get Started"

5. Create `src/pages/NotFoundPage.tsx`:
   - title="Page Not Found | Omer Etrog"
   - h1="Page Not Found"
   - Link back to home using `<Link to="/">`

6. Create `src/router.tsx` using `createBrowserRouter` (Data Mode per research recommendation):
   - Root layout route with `<RootLayout />` as element
   - Children: `/` -> HomePage, `/work` -> WorkPage, `/about` -> AboutPage, `/contact` -> ContactPage, `*` -> NotFoundPage

7. Update `src/main.tsx` to use RouterProvider:
   ```tsx
   import React from "react";
   import ReactDOM from "react-dom/client";
   import { RouterProvider } from "react-router";
   import { router } from "./router";
   import "./styles/index.css";

   ReactDOM.createRoot(document.getElementById("root")!).render(
     <React.StrictMode>
       <RouterProvider router={router} />
     </React.StrictMode>
   );
   ```

All colors MUST reference design tokens (bg-bg, text-text, text-text-muted, text-accent, bg-surface, border-border). NEVER hardcode hex values in components.
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 pages render inside the shared layout. Nav shows links to all pages with active styling. Footer appears on every page. Each page has unique title, meta description, canonical URL, and exactly one h1. Mobile nav toggle works. 404 page renders for unknown routes.</done>
</task>

<task type="auto">
  <name>Task 2: Create test suite for Nav, Layout, SEO, and build outputs</name>
  <files>tests/nav.test.tsx, tests/seo.test.tsx, tests/layout.test.tsx</files>
  <action>
1. Create `tests/nav.test.tsx`:
   ```tsx
   import { describe, it, expect } from "vitest";
   import { render, screen } from "@testing-library/react";
   import { createMemoryRouter, RouterProvider } from "react-router";
   import Nav from "../src/components/layout/Nav";
   import RootLayout from "../src/components/layout/RootLayout";

   function renderWithRouter(initialEntry = "/") {
     const router = createMemoryRouter(
       [{ element: <RootLayout />, children: [{ path: "*", element: <div>Test</div> }] }],
       { initialEntries: [initialEntry] }
     );
     return render(<RouterProvider router={router} />);
   }

   describe("Nav", () => {
     it("renders links to all 4 pages", () => {
       renderWithRouter();
       const links = screen.getAllByRole("link");
       const hrefs = links.map((l) => l.getAttribute("href"));
       expect(hrefs).toContain("/");
       expect(hrefs).toContain("/work");
       expect(hrefs).toContain("/about");
       expect(hrefs).toContain("/contact");
     });

     it("has main navigation aria-label", () => {
       renderWithRouter();
       expect(screen.getByRole("navigation", { name: /main/i })).toBeInTheDocument();
     });
   });
   ```

2. Create `tests/layout.test.tsx`:
   ```tsx
   import { describe, it, expect } from "vitest";
   import { render, screen } from "@testing-library/react";
   import { createMemoryRouter, RouterProvider } from "react-router";
   import RootLayout from "../src/components/layout/RootLayout";

   function renderLayout() {
     const router = createMemoryRouter(
       [{ element: <RootLayout />, children: [{ path: "*", element: <div>Page Content</div> }] }],
       { initialEntries: ["/"] }
     );
     return render(<RouterProvider router={router} />);
   }

   describe("RootLayout", () => {
     it("renders navigation", () => {
       renderLayout();
       expect(screen.getByRole("navigation")).toBeInTheDocument();
     });

     it("renders footer with contentinfo role", () => {
       renderLayout();
       expect(screen.getByRole("contentinfo")).toBeInTheDocument();
     });

     it("renders main landmark", () => {
       renderLayout();
       expect(screen.getByRole("main")).toBeInTheDocument();
     });

     it("renders page content inside main via Outlet", () => {
       renderLayout();
       expect(screen.getByText("Page Content")).toBeInTheDocument();
     });
   });
   ```

3. Create `tests/seo.test.tsx` that tests each page has a unique title and exactly one h1:
   ```tsx
   import { describe, it, expect } from "vitest";
   import { render, screen } from "@testing-library/react";
   import { createMemoryRouter, RouterProvider } from "react-router";
   import RootLayout from "../src/components/layout/RootLayout";
   import HomePage from "../src/pages/HomePage";
   import WorkPage from "../src/pages/WorkPage";
   import AboutPage from "../src/pages/AboutPage";
   import ContactPage from "../src/pages/ContactPage";

   const pages = [
     { path: "/", element: <HomePage />, name: "Home" },
     { path: "/work", element: <WorkPage />, name: "Work" },
     { path: "/about", element: <AboutPage />, name: "About" },
     { path: "/contact", element: <ContactPage />, name: "Contact" },
   ];

   function renderPage(path: string) {
     const routes = pages.map((p) => ({ path: p.path, element: p.element }));
     const router = createMemoryRouter(
       [{ element: <RootLayout />, children: routes }],
       { initialEntries: [path] }
     );
     return render(<RouterProvider router={router} />);
   }

   describe("SEO — per-page metadata", () => {
     pages.forEach(({ path, name }) => {
       describe(name, () => {
         it("has exactly one h1", () => {
           const { container } = renderPage(path);
           const h1s = container.querySelectorAll("h1");
           expect(h1s.length).toBe(1);
         });
       });
     });
   });

   describe("SEO — semantic landmarks", () => {
     it("every page has a section with aria-labelledby", () => {
       pages.forEach(({ path }) => {
         const { container } = renderPage(path);
         const sections = container.querySelectorAll("section[aria-labelledby]");
         expect(sections.length).toBeGreaterThanOrEqual(1);
       });
     });
   });
   ```

   Note: React 19 `<title>` hoisting may not work in jsdom/Vitest since it requires a browser-like document. The h1 and semantic landmark tests are the reliable automated checks. Title uniqueness is verified by inspecting the JSX source (each page file contains a different `<title>` string).

4. Run the full test suite to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All test files pass: design-tokens.test.ts, nav.test.tsx, layout.test.tsx, seo.test.tsx. Tests verify: Nav has links to all 4 pages, layout has nav + footer + main landmarks, every page has exactly one h1, and all design tokens are defined.</done>
</task>

<task type="auto">
  <name>Task 3: Verify sitemap.xml and robots.txt generation at build time</name>
  <files>tests/build-outputs.test.ts</files>
  <action>
1. Create `tests/build-outputs.test.ts`:
   ```typescript
   import { describe, it, expect, beforeAll } from "vitest";
   import { existsSync, readFileSync } from "fs";
   import { resolve } from "path";
   import { execSync } from "child_process";

   const distDir = resolve(__dirname, "../dist");

   // Build once before all tests in this file
   beforeAll(() => {
     execSync("npm run build", { cwd: resolve(__dirname, ".."), stdio: "pipe" });
   }, 60000);

   describe("Build outputs — SEO-04", () => {
     it("generates sitemap.xml in dist/", () => {
       expect(existsSync(resolve(distDir, "sitemap.xml"))).toBe(true);
     });

     it("sitemap.xml contains all 4 routes", () => {
       const sitemap = readFileSync(resolve(distDir, "sitemap.xml"), "utf-8");
       expect(sitemap).toContain("https://omeretrog.com/");
       expect(sitemap).toContain("https://omeretrog.com/work");
       expect(sitemap).toContain("https://omeretrog.com/about");
       expect(sitemap).toContain("https://omeretrog.com/contact");
     });

     it("generates robots.txt in dist/", () => {
       expect(existsSync(resolve(distDir, "robots.txt"))).toBe(true);
     });

     it("robots.txt references the sitemap", () => {
       const robots = readFileSync(resolve(distDir, "robots.txt"), "utf-8");
       expect(robots.toLowerCase()).toContain("sitemap");
     });

     it("_redirects file is copied to dist/", () => {
       expect(existsSync(resolve(distDir, "_redirects"))).toBe(true);
     });
   });
   ```

2. Run this specific test file (it triggers a build, so run it alone):
   ```bash
   npx vitest run tests/build-outputs.test.ts --reporter=verbose
   ```

If `vite-plugin-sitemap` does not generate robots.txt by default, create a static `public/robots.txt` with:
```
User-agent: *
Allow: /
Sitemap: https://omeretrog.com/sitemap.xml
```
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npx vitest run tests/build-outputs.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Build produces dist/sitemap.xml with all 4 routes, dist/robots.txt referencing the sitemap, and dist/_redirects with the SPA fallback rule.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx vitest run` — all tests green (design-tokens, nav, layout, seo, build-outputs)
- Navigate to each page via React Router: /, /work, /about, /contact
- Each page shows unique h1 text
- Nav highlights the active page link
- Mobile hamburger toggle shows/hides nav links
- Footer visible on every page
- `dist/sitemap.xml` contains all 4 route URLs
- `dist/robots.txt` references the sitemap
</verification>

<success_criteria>
- All 4 pages are routable and render inside the shared layout
- Nav has links to Home, Work, About, Contact with active state highlighting
- Footer appears on every page with contact info and social links
- Each page has unique, keyword-targeted title and meta description
- Every page has exactly one h1 and uses semantic HTML (section, nav, main, footer landmarks)
- sitemap.xml and robots.txt generated correctly at build time
- Full test suite passes (all test files green)
- Layout is responsive (mobile hamburger nav, flexible grid)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
