---
phase: 04-lead-generation-polish-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ScrollReveal.tsx
  - src/components/layout/RootLayout.tsx
  - src/components/home/HeroSection.tsx
  - src/components/home/HowItWorksSection.tsx
  - src/components/home/TestimonialsSection.tsx
  - src/components/about/AboutHero.tsx
  - src/components/work/PortfolioItem.tsx
  - tests/scroll-reveal.test.tsx
  - tests/layout.test.tsx
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03]

must_haves:
  truths:
    - "Sections animate in with fade-up effect when scrolling into view"
    - "Animations only play once per section (not on every scroll)"
    - "Enabling OS Reduce Motion suppresses all transform animations"
    - "Animated properties are only opacity and transform (y) — no layout properties"
  artifacts:
    - path: "src/components/ui/ScrollReveal.tsx"
      provides: "Reusable motion.div wrapper with whileInView entrance animation"
      min_lines: 15
    - path: "src/components/layout/RootLayout.tsx"
      provides: "MotionConfig reducedMotion='user' wrapping entire app"
    - path: "tests/scroll-reveal.test.tsx"
      provides: "Unit tests for ScrollReveal animation props"
    - path: "tests/layout.test.tsx"
      provides: "Updated test asserting MotionConfig is present"
  key_links:
    - from: "src/components/layout/RootLayout.tsx"
      to: "motion/react"
      via: "MotionConfig import"
      pattern: "MotionConfig.*reducedMotion.*user"
    - from: "src/components/ui/ScrollReveal.tsx"
      to: "motion/react"
      via: "motion.div with whileInView"
      pattern: "whileInView"
    - from: "src/components/home/HowItWorksSection.tsx"
      to: "src/components/ui/ScrollReveal.tsx"
      via: "import and wrap section content"
      pattern: "import.*ScrollReveal"
---

<objective>
Add scroll-triggered entrance animations to all major content sections using the Motion library, with a global MotionConfig that respects OS reduced-motion preference, using only compositor-safe properties (transform + opacity).

Purpose: Polish pass that makes the site feel premium — sections fade up as the visitor scrolls, but animations are suppressed for users who prefer reduced motion.
Output: ScrollReveal reusable component, MotionConfig in RootLayout, all page sections wrapped, tests updated.
</objective>

<execution_context>
@/Users/omere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lead-generation-polish-launch/04-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs. -->

From src/components/layout/RootLayout.tsx:
```typescript
// Current: div > a(skip-to-content) + Nav + main(Outlet) + Footer
// Must add: MotionConfig reducedMotion="user" wrapping the outer div
import { Outlet } from "react-router";
import Nav from "./Nav";
import Footer from "./Footer";
```

From src/components/home/HeroSection.tsx (section component pattern):
```typescript
// All section components follow: <section aria-labelledby="..."> with content inside
// ScrollReveal wraps CONTENT inside sections, not the <section> itself
// This preserves semantic HTML landmarks
```

From project patterns:
- Section components receive typed props (e.g., HowItWorksSection receives steps prop)
- motion package imports from "motion/react" (not "framer-motion")
- Tests use createMemoryRouter + RouterProvider for routing context
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ScrollReveal component + add MotionConfig to RootLayout + tests</name>
  <files>
    src/components/ui/ScrollReveal.tsx,
    src/components/layout/RootLayout.tsx,
    tests/scroll-reveal.test.tsx,
    tests/layout.test.tsx
  </files>
  <behavior>
    - ScrollReveal renders children inside a motion.div
    - ScrollReveal's motion.div has initial={{ opacity: 0, y: 24 }}
    - ScrollReveal's motion.div has whileInView={{ opacity: 1, y: 0 }}
    - ScrollReveal's motion.div has viewport={{ once: true, amount: 0.15 }}
    - ScrollReveal accepts optional delay and className props
    - ScrollReveal does NOT animate any layout properties (height, width, margin, padding, top, left)
    - RootLayout wraps content in MotionConfig with reducedMotion="user"
  </behavior>
  <action>
    1. Note: `motion` package is installed in Plan 01 Task 1 (same wave — executor may need to run `npm install motion` if executing this plan first).

    2. Create `src/components/ui/ScrollReveal.tsx`:
       - Import `motion` from `"motion/react"`
       - Props interface: `{ children: React.ReactNode; delay?: number; className?: string }`
       - Return `<motion.div initial={{ opacity: 0, y: 24 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.15 }} transition={{ duration: 0.5, delay, ease: "easeOut" }} className={className}>{children}</motion.div>`
       - CRITICAL: Only `opacity` and `y` (transform: translateY) — never height, width, margin, padding, top, left

    3. Update `src/components/layout/RootLayout.tsx`:
       - Import `MotionConfig` from `"motion/react"`
       - Wrap the outermost `<div>` content with `<MotionConfig reducedMotion="user">...</MotionConfig>`
       - Keep all existing structure (skip-to-content, Nav, main, Footer) unchanged

    4. Write `tests/scroll-reveal.test.tsx`:
       - Mock `motion/react` with vi.mock: mock `motion` as object with `div` being a forwardRef that renders a `<div>` passing through data-testid, children, className, and stores initial/whileInView as data attributes for assertion
       - Test: renders children inside the wrapper
       - Test: passes className to motion.div
       - Test: initial prop contains only `opacity` and `y` keys (no layout props)
       - Test: whileInView prop contains only `opacity` and `y` keys (no layout props)

    5. Update `tests/layout.test.tsx`:
       - Mock `motion/react` with vi.mock: MotionConfig renders children, motion.div renders children
       - Add test: "wraps content with MotionConfig reducedMotion='user'" — render RootLayout, check that MotionConfig was called with reducedMotion="user" (use vi.mock to capture props)
       - Keep all existing layout tests passing
  </action>
  <verify>
    <automated>npx vitest run tests/scroll-reveal.test.tsx tests/layout.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ScrollReveal component uses only opacity+y, MotionConfig with reducedMotion="user" wraps RootLayout. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Apply ScrollReveal to all page sections</name>
  <files>
    src/components/home/HeroSection.tsx,
    src/components/home/HowItWorksSection.tsx,
    src/components/home/TestimonialsSection.tsx,
    src/components/about/AboutHero.tsx,
    src/components/work/PortfolioItem.tsx
  </files>
  <action>
    Wrap the main content blocks (NOT the outer `<section>` element) in ScrollReveal for each component:

    1. `src/components/home/HeroSection.tsx`:
       - Do NOT wrap the hero section itself (it's above the fold, should be visible immediately)
       - If there is a secondary content block below the fold in the hero, wrap that in ScrollReveal
       - If the entire hero is above-fold content, skip ScrollReveal here entirely (hero must not be delayed — it's the LCP element)

    2. `src/components/home/HowItWorksSection.tsx`:
       - Import ScrollReveal from `../ui/ScrollReveal`
       - Wrap the steps grid content in `<ScrollReveal>`
       - Keep the `<section>` and `<h2>` outside ScrollReveal (heading should be visible for landmark navigation)

    3. `src/components/home/TestimonialsSection.tsx`:
       - Import ScrollReveal
       - Wrap the testimonial cards container in `<ScrollReveal>`
       - Keep section heading outside ScrollReveal

    4. `src/components/about/AboutHero.tsx`:
       - Import ScrollReveal
       - Wrap the bio content (not the h1) in `<ScrollReveal>`
       - h1 stays outside so the page heading is immediately visible

    5. `src/components/work/PortfolioItem.tsx`:
       - Import ScrollReveal
       - Wrap each portfolio item's `<article>` content in `<ScrollReveal>`
       - Each item animates in independently as user scrolls to it

    IMPORTANT: Apply ScrollReveal at the SECTION level, not per-element. Stay under 10 simultaneous animated elements. One ScrollReveal per section is the target.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All major page sections (HowItWorks, Testimonials, AboutHero, PortfolioItem) use ScrollReveal. Hero section does NOT delay its content. Full test suite passes (existing tests unbroken).</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
</verification>

<success_criteria>
- ScrollReveal component animates only opacity and y (transform)
- MotionConfig reducedMotion="user" wraps the entire app in RootLayout
- Content sections fade up on scroll entry, playing once only
- Hero section is NOT delayed by ScrollReveal (above-fold content stays instant)
- All existing tests continue to pass
- New tests verify animation prop constraints and MotionConfig presence
</success_criteria>

<output>
After completion, create `.planning/phases/04-lead-generation-polish-launch/04-02-SUMMARY.md`
</output>
