---
phase: 04-lead-generation-polish-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/contact/ContactForm.tsx
  - src/components/contact/CalendlyEmbed.tsx
  - src/pages/ContactPage.tsx
  - tests/contact-form.test.tsx
  - tests/calendly-embed.test.tsx
  - tests/contact-page.test.tsx
autonomous: true
requirements: [LEAD-01, LEAD-03, LEAD-04, LEAD-05]

must_haves:
  truths:
    - "Contact page renders an inquiry form with name, email, message, and current site URL fields"
    - "Form shows success confirmation after successful submission"
    - "Form shows error state when submission fails"
    - "Honeypot field is hidden but present for spam filtering"
    - "Calendly booking calendar is available on contact page via facade pattern"
  artifacts:
    - path: "src/components/contact/ContactForm.tsx"
      provides: "Inquiry form with Formspree useForm, honeypot, Turnstile, success/error states"
      min_lines: 50
    - path: "src/components/contact/CalendlyEmbed.tsx"
      provides: "Calendly InlineWidget with facade click-to-load pattern"
      min_lines: 20
    - path: "src/pages/ContactPage.tsx"
      provides: "Contact page composing ContactForm + CalendlyEmbed"
      min_lines: 20
    - path: "tests/contact-form.test.tsx"
      provides: "Unit tests for ContactForm: fields, honeypot, success/error states"
    - path: "tests/calendly-embed.test.tsx"
      provides: "Unit tests for CalendlyEmbed: facade toggle behavior"
    - path: "tests/contact-page.test.tsx"
      provides: "Integration test for ContactPage field presence"
  key_links:
    - from: "src/pages/ContactPage.tsx"
      to: "src/components/contact/ContactForm.tsx"
      via: "import and render"
      pattern: "import.*ContactForm"
    - from: "src/pages/ContactPage.tsx"
      to: "src/components/contact/CalendlyEmbed.tsx"
      via: "import and render"
      pattern: "import.*CalendlyEmbed"
    - from: "src/components/contact/ContactForm.tsx"
      to: "@formspree/react"
      via: "useForm hook"
      pattern: "useForm"

user_setup:
  - service: formspree
    why: "Email delivery for contact form submissions"
    env_vars:
      - name: VITE_FORMSPREE_FORM_ID
        source: "Formspree Dashboard -> Forms -> Create Form -> Copy hashid"
    dashboard_config:
      - task: "Create a form project"
        location: "https://formspree.io -> New Form -> Copy Form ID"
  - service: cloudflare-turnstile
    why: "Invisible spam challenge on contact form"
    env_vars:
      - name: VITE_TURNSTILE_SITE_KEY
        source: "Cloudflare Dashboard -> Turnstile -> Add Site -> Copy Site Key"
    dashboard_config:
      - task: "Register domain for Turnstile"
        location: "https://dash.cloudflare.com -> Turnstile -> Add Widget -> domain: omeretrog.com"
  - service: calendly
    why: "Booking calendar embed on contact page"
    env_vars: []
    dashboard_config:
      - task: "Get scheduling URL"
        location: "Calendly Dashboard -> Event Types -> Copy link (e.g., https://calendly.com/omeretrog/30min)"
---

<objective>
Build the contact page with a working inquiry form (Formspree), spam protection (honeypot + Turnstile), success/error states, and an embedded Calendly booking calendar via facade pattern.

Purpose: This is the primary lead generation mechanism — visitors who want to hire Omer need two clear paths: fill out a form or book a call directly.
Output: ContactForm component, CalendlyEmbed component, updated ContactPage, and full test coverage.
</objective>

<execution_context>
@/Users/omere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lead-generation-polish-launch/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/pages/ContactPage.tsx (current stub — will be replaced):
```typescript
// Current: bare section with h1 and intro paragraph. No form, no calendar.
// Must preserve: SEO title/meta/canonical, h1 heading, intro paragraph
```

From project patterns (history digest):
- Thin composer pattern: page imports components + data, passes props, adds SEO tags
- Section components receive typed props for testability
- Content data in src/content/ with TypeScript interfaces
- Tests use createMemoryRouter for routing context
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies + create ContactForm and CalendlyEmbed components with tests</name>
  <files>
    src/components/contact/ContactForm.tsx,
    src/components/contact/CalendlyEmbed.tsx,
    tests/contact-form.test.tsx,
    tests/calendly-embed.test.tsx
  </files>
  <behavior>
    - ContactForm renders inputs for name, email, message, currentSite (all with labels)
    - ContactForm renders honeypot input named "_gotcha" with opacity:0, position:absolute, pointer-events:none, tabIndex:-1, aria-hidden:true, autocomplete:off
    - ContactForm renders Turnstile component inside the form element
    - ContactForm renders submit button with text "Send Message"
    - ContactForm shows "Sending..." text on submit button when state.submitting is true
    - ContactForm shows success message (role="status") when state.succeeded is true
    - ContactForm shows error message (role="alert") when submission errors exist
    - ContactForm disables submit button while submitting
    - CalendlyEmbed renders facade button "Open Booking Calendar" by default (no InlineWidget loaded)
    - CalendlyEmbed renders InlineWidget after facade button is clicked
  </behavior>
  <action>
    1. Install dependencies: `npm install @formspree/react motion react-calendly @marsidev/react-turnstile`

    2. Create `src/components/contact/ContactForm.tsx`:
       - Import `useForm`, `ValidationError` from `@formspree/react`
       - Import `Turnstile` from `@marsidev/react-turnstile`
       - Use `useForm(import.meta.env.VITE_FORMSPREE_FORM_ID || "test")` — fallback to "test" for dev
       - If `state.succeeded`, return success message: `<p role="status">Thanks! Omer will be in touch within 24 hours.</p>`
       - Render form with `onSubmit={handleSubmit}` and `noValidate`
       - Honeypot: `<input type="text" name="_gotcha" style={{ opacity: 0, position: "absolute", pointerEvents: "none" }} tabIndex={-1} autoComplete="off" aria-hidden="true" />`
       - Fields: name (text, required), email (email, required + ValidationError), message (textarea, required + ValidationError), currentSite (url, optional)
       - Turnstile inside form: `<Turnstile siteKey={import.meta.env.VITE_TURNSTILE_SITE_KEY || "1x00000000000000000000AA"} options={{ size: "invisible" }} />`
       - Submit button: disabled when `state.submitting`, text toggles "Sending..." / "Send Message"
       - Generic error fallback: `<p role="alert">` when errors exist
       - Style with Tailwind: dark inputs (bg-white/5 border-white/10), focus ring accent color, rounded-lg, proper spacing

    3. Create `src/components/contact/CalendlyEmbed.tsx`:
       - Accept `url` prop (string)
       - State: `loaded` (boolean, default false)
       - If not loaded: render facade div with "Prefer to book a time directly?" text and "Open Booking Calendar" button
       - On button click: set loaded = true
       - If loaded: render `<InlineWidget url={url} styles={{ height: "700px" }} pageSettings={{ backgroundColor: "111111", textColor: "ffffff", primaryColor: "6366f1" }} />`
       - Style facade: rounded-lg, border border-white/10, p-8, text-center

    4. Write tests in `tests/contact-form.test.tsx`:
       - Mock `@formspree/react` with vi.mock: return configurable `useForm` mock (default: submitting=false, succeeded=false, errors=[])
       - Mock `@marsidev/react-turnstile` with vi.mock: render a hidden input
       - Test: renders all four labeled fields (name, email, message, currentSite)
       - Test: honeypot input has name="_gotcha", aria-hidden="true", tabIndex=-1
       - Test: submit button says "Send Message" in default state
       - Test: submit button says "Sending..." and is disabled when submitting=true
       - Test: success message with role="status" shown when succeeded=true
       - Test: error message with role="alert" shown when errors are non-empty

    5. Write tests in `tests/calendly-embed.test.tsx`:
       - Mock `react-calendly` with vi.mock: render a div with data-testid="calendly-widget"
       - Test: renders facade button "Open Booking Calendar" by default
       - Test: does NOT render InlineWidget before click
       - Test: renders InlineWidget after clicking facade button
  </action>
  <verify>
    <automated>npx vitest run tests/contact-form.test.tsx tests/calendly-embed.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ContactForm renders all fields with honeypot and Turnstile, shows success/error states. CalendlyEmbed uses facade pattern. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ContactPage as thin composer with ContactForm + CalendlyEmbed + page-level test</name>
  <files>
    src/pages/ContactPage.tsx,
    tests/contact-page.test.tsx
  </files>
  <behavior>
    - ContactPage renders h1 "Get Started"
    - ContactPage renders ContactForm component
    - ContactPage renders CalendlyEmbed component
    - ContactPage has two distinct sections: form and calendar
    - All four form fields (name, email, message, currentSite) are present in the rendered page
  </behavior>
  <action>
    1. Update `src/pages/ContactPage.tsx`:
       - Keep existing SEO meta (title, description, canonical), h1, intro paragraph
       - Import ContactForm from `../components/contact/ContactForm`
       - Import CalendlyEmbed from `../components/contact/CalendlyEmbed`
       - Below intro paragraph, add two-column layout (stacked on mobile, side-by-side on lg):
         - Left/top: `<ContactForm />` with heading "Send a Message" (h2)
         - Right/bottom: `<CalendlyEmbed url="https://calendly.com/omeretrog/30min" />` with heading "Book a Call" (h2)
       - Use Tailwind grid: `grid lg:grid-cols-2 gap-12 mt-12`
       - Page stays thin composer pattern — no business logic

    2. Write tests in `tests/contact-page.test.tsx`:
       - Mock `@formspree/react` (same as contact-form test)
       - Mock `@marsidev/react-turnstile`
       - Mock `react-calendly`
       - Render ContactPage inside createMemoryRouter (same pattern as other page tests)
       - Test: renders h1 "Get Started"
       - Test: renders "Send a Message" h2
       - Test: renders "Book a Call" h2
       - Test: renders all four form input fields by label text
       - Test: renders booking calendar facade button
  </action>
  <verify>
    <automated>npx vitest run tests/contact-page.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ContactPage composes both ContactForm and CalendlyEmbed in a responsive layout. Page test confirms all fields present and both sections render.</done>
</task>

</tasks>

<verification>
npx vitest run tests/contact-form.test.tsx tests/calendly-embed.test.tsx tests/contact-page.test.tsx --reporter=verbose
</verification>

<success_criteria>
- Contact form renders name, email, message, currentSite fields with proper labels
- Honeypot field named "_gotcha" is hidden but present in DOM
- Turnstile component renders inside the form
- Success and error states display with correct ARIA roles
- Calendly facade loads InlineWidget only on click (not on mount)
- ContactPage composes both components in responsive two-column layout
- All new tests pass alongside existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-lead-generation-polish-launch/04-01-SUMMARY.md`
</output>
