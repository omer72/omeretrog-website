---
phase: 02-core-pages-content
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/about/AboutHero.tsx
  - src/pages/AboutPage.tsx
  - tests/about-page.test.tsx
  - public/omer-placeholder.svg
autonomous: true
requirements: [ABOUT-01, ABOUT-02]

must_haves:
  truths:
    - "About page presents Omer's background and expertise with real content"
    - "About page displays a professional photo or branded placeholder element"
    - "About page does not feel anonymous — has name, photo, and specific background"
  artifacts:
    - path: "src/components/about/AboutHero.tsx"
      provides: "About section with bio content and photo"
      min_lines: 25
    - path: "src/pages/AboutPage.tsx"
      provides: "Thin page composer for About"
      min_lines: 15
    - path: "tests/about-page.test.tsx"
      provides: "Tests for ABOUT-01 and ABOUT-02"
    - path: "public/omer-placeholder.svg"
      provides: "Dark-themed placeholder with initials until real photo available"
  key_links:
    - from: "src/pages/AboutPage.tsx"
      to: "src/components/about/AboutHero.tsx"
      via: "import and render"
      pattern: "import AboutHero"
---

<objective>
Build the About page with Omer's background, expertise, and a professional photo/branding element. Write tests for ABOUT-01 and ABOUT-02.

Purpose: Service buyers need to trust the person behind the service. An anonymous About page kills conversions. This page establishes Omer's credibility.
Output: AboutHero component, updated AboutPage, test file, placeholder photo.
</objective>

<execution_context>
@/Users/omere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages-content/02-RESEARCH.md

@src/styles/index.css
@src/pages/AboutPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AboutHero component, update AboutPage, and write tests</name>
  <files>src/components/about/AboutHero.tsx, src/pages/AboutPage.tsx, tests/about-page.test.tsx, public/omer-placeholder.svg</files>
  <behavior>
    - AboutPage renders an h1 heading with "Omer" in it (ABOUT-01)
    - AboutPage renders body text about background/expertise (ABOUT-01)
    - AboutPage renders an img element with alt text (ABOUT-02)
    - AboutPage mentions specific skills/experience (not generic "I build websites")
  </behavior>
  <action>
Write test FIRST (RED), then implement (GREEN).

**Test (write first):**

`tests/about-page.test.tsx`:
- Test 1: Renders h1 containing "Omer" (ABOUT-01).
- Test 2: Renders body text about expertise — check for keywords like "migration" or "modern web" (ABOUT-01).
- Test 3: Renders an `img` element with non-empty `alt` attribute (ABOUT-02).
- Test 4: Renders specific background detail (not just "web developer") — check for "Wix" or "migration" in body text.
- Wrap in MemoryRouter if any Link components used.

**Placeholder photo:**

Create `public/omer-placeholder.svg`: A dark-themed SVG (400x400) with `#1a1a1a` background, `#333333` circle, and the initials "OE" in the accent color. Add a comment: `<!-- TODO: Replace with real professional photo (omer.jpg) before launch -->`.

**AboutHero component:**

`src/components/about/AboutHero.tsx`:
- Two-column layout on desktop (`grid md:grid-cols-2 gap-12 items-center`), stacked on mobile.
- Left column: Photo — `<img src="/omer-placeholder.svg" alt="Omer Etrog, web migration specialist" className="rounded-2xl w-full max-w-sm mx-auto" />`. Add `// TODO: Replace src with "/omer.jpg" when real photo is available` comment.
- Right column: Bio content.
  - h2: "Hey, I'm Omer" (or similar personal greeting).
  - 2-3 paragraphs covering: (1) What he does — specializes in migrating old Wix sites to modern, fast websites, (2) His approach — speed and completeness, full content transfer in under an hour, (3) Why he does it — belief that every business deserves a modern web presence.
  - Use `text-text-muted` for body paragraphs, `text-text` for emphasis lines.
  - Include specific details: "Wix migration", "under an hour", "modern codebase" — not generic freelancer copy.
- Section wrapper: `py-16 md:py-24`.

**AboutPage update:**

Rewrite `src/pages/AboutPage.tsx` as thin composer:
- Keep existing `<title>`, `<meta name="description">`, `<link rel="canonical">` tags.
- Import and render `AboutHero`.
- h1 stays: "About Omer" — but move it inside AboutHero or keep in page (executor's choice, as long as test passes).
- File should be under 25 lines.
  </action>
  <verify>
    <automated>cd /Users/omere/projects/omeretrog-website && npx vitest run tests/about-page.test.tsx && npx vitest run</automated>
  </verify>
  <done>About page renders Omer's background with specific expertise details (ABOUT-01). Photo/branding element renders as img with alt text (ABOUT-02). All tests pass including full Phase 1 + Phase 2 suite.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/about-page.test.tsx` — all about tests green
- `npx vitest run` — full suite passes
- AboutPage.tsx is under 25 lines
- img element has meaningful alt text
</verification>

<success_criteria>
- About page shows Omer's name, photo/placeholder, and specific background details
- Content mentions Wix migration and speed differentiator (not generic copy)
- Professional photo placeholder exists with clear TODO for replacement
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-content/02-03-SUMMARY.md`
</output>
